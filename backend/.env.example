APP_ENV=development
APP_DEBUG=true

DB_HOST=localhost
DB_PORT=5432
DB_NAME=xestify_dev
DB_USER=postgres
DB_PASSWORD=change_me

JWT_SECRET=change_me_random_secret_min_32_chars
JWT_EXPIRY=3600

# Optional: full path to the psql binary, only needed for
# MigrationIdempotenceTest.php when psql isn't on PATH (e.g. Windows).
# PSQL_PATH=C:\Program Files\PostgreSQL\18\bin\psql.exe
